import environment from './environment';

export const routes = {
  ARCHITECTURE_DIAGRAM: `${environment.FRONTEND_URL}/diagram`,
  MOCK_INTERVIEW: `${environment.FRONTEND_URL}/mockinterview`,
  PRESENTATION_SLIDES: `${environment.FRONTEND_URL}/slides`,
};
